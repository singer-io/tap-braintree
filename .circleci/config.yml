version: 2.1

executors:
  docker-executor:
    docker:
      - image: 218546966473.dkr.ecr.us-east-1.amazonaws.com/circle-ci:stitch-tap-tester

jobs:
  build:
    executor: docker-executor
    steps:
      - checkout
      - run:
          name: 'Setup'
          command: |
            python3 -m venv /usr/local/share/virtualenvs/tap-braintree
            source /usr/local/share/virtualenvs/tap-braintree/bin/activate
            pip install -U 'pip<19.2' setuptools
            pip install .[dev]
      - run:
          name: 'Pylint'
          command: |
            source /usr/local/share/virtualenvs/tap-braintree/bin/activate
            pylint tap_braintree -d C,R,W
      - run:
          name: 'JSON Validator'
          command: |
            source /usr/local/share/virtualenvs/tap-braintree/bin/activate
            stitch-validate-json tap_braintree/schemas/*.json
      - run:
          name: 'Unit Tests'
          command: |
            source /usr/local/share/virtualenvs/tap-braintree/bin/activate
            nosetests tests
workflows:
  version: 2
  commit:
    jobs:
      - build:
          context: circleci-user
